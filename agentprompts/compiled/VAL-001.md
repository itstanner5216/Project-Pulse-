# Task Execution Prompt Template for GitHub Copilot Agent

**Instructions for Human Operator:**
1. Copy this entire template
2. Replace all `{{VARIABLES}}` with actual values from QUEUE.yml
3. Paste the completed prompt into GitHub Copilot Agent
4. Review the agent's plan before accepting
5. Monitor execution and review all changes

---

## Task Context

**Repository:** itstanner5216/Project-Pulse-  
**Current Branch:** `copilot-queue-tasks`  
**Base Branch:** `main`  
**Task ID:** `VAL-001`  
**Task Title:** `Validate Agent Type at Runtime`

---

## Your Mission

You are a GitHub Copilot coding agent working on a task from a sequential task queue. Your goal is to implement **exactly one specific task** and nothing more. This is part of a larger effort tracked in `agentprompts/QUEUE.yml`.

### Critical Constraints

**DO:**
- ✅ Work ONLY on the current branch: `copilot-queue-tasks`
- ✅ Implement ONLY the task specified below
- ✅ Follow the acceptance criteria exactly
- ✅ Write or update tests as required
- ✅ Update relevant documentation
- ✅ Use the exact commit message provided
- ✅ Update the task status in QUEUE.yml to "done" after completion
- ✅ Make minimal, surgical changes
- ✅ Verify your changes don't break existing functionality

**DO NOT:**
- ❌ Create or switch to a different branch
- ❌ Open a new pull request
- ❌ Work on other tasks from the queue
- ❌ Make unrelated changes or "improvements"
- ❌ Skip writing tests
- ❌ Break existing functionality
- ❌ Commit multiple unrelated changes together

---

## Task Details

### Description

```
Add runtime validation to ensure agent types are valid before attempting 
to load agent prompt files.
```

### Source References

Review these documents for complete context before starting:

```
- CODE_REVIEW_FINDINGS.md#6-no-validation-of-agent-type-at-runtime
- FOLLOWUP_TASKS.md#task-6-validate-agent-type-at-runtime
```

Read the specific sections in these files to understand:
- The problem being solved
- Why this is important
- Potential edge cases
- Suggested implementation approach

### Acceptance Criteria

Your implementation will be considered complete when ALL of the following criteria are met:

```
- [ ] Add validation at start of loadAgentPrompt() function
- [ ] Throw descriptive error if agent type is invalid
- [ ] List valid agent types in error message
- [ ] Create isValidAgentType() type guard function (optional)
- [ ] Add unit tests for invalid agent types
- [ ] Update documentation with list of valid agent types
```

Check each criterion carefully after implementing. If you cannot meet a criterion, document why in your response.

---

## Implementation Guidelines

### 1. Understand the Problem First

Before writing any code:
- Read all source reference documents
- Understand the root cause of the issue
- Identify all affected files
- Consider edge cases and error handling
- Plan your testing strategy

### 2. Code Quality Standards

Your code must:
- Follow existing code style in the repository
- Use TypeScript strict typing (no `any` types without justification)
- Include proper error handling with descriptive messages
- Add JSDoc/TSDoc comments for new public functions
- Handle edge cases identified in source documents
- Be consistent with patterns used in the codebase

### 3. Testing Requirements

Based on the task priority, provide appropriate test coverage:

**Critical/High Priority Tasks:**
- Comprehensive unit tests covering normal cases, edge cases, and error cases
- Integration tests if the change affects multiple components
- Security tests for security-related changes
- Minimum 90% coverage for changed code

**Medium Priority Tasks:**
- Unit tests covering normal cases and common edge cases
- At least one error case test
- Minimum 80% coverage for changed code

**Low Priority Tasks:**
- Unit tests covering the main functionality
- At least one edge case
- Minimum 70% coverage for changed code

### 4. Documentation Updates

Update documentation if your changes:
- Modify public APIs or function signatures
- Change configuration options
- Affect security behavior
- Introduce new features or capabilities
- Change error messages or logging

Files that may need updates:
- Inline code comments (JSDoc/TSDoc)
- README.md (if user-facing changes)
- CONTRIBUTING.md (if development workflow changes)
- SECURITY.md (if security-related changes)

---

## Commit Instructions

### Commit Message Format

Use EXACTLY this commit message (provided in QUEUE.yml):

```
fix: add runtime validation for agent types
```

### What to Commit

Stage and commit:
- All source code changes
- All test files (new or modified)
- Documentation updates
- The updated QUEUE.yml file (with status changed to "done")

Do NOT commit:
- Build artifacts (dist/, build/, node_modules/, etc.)
- IDE configuration files (.vscode/, .idea/, etc.)
- Temporary files
- Unrelated changes

### Commit Process

```bash
# Review your changes
git status
git diff

# Stage the specific files you modified
git add <file1> <file2> <file3>

# Commit with the exact message from above
git commit -m "fix: add runtime validation for agent types"

# Update QUEUE.yml status
# (Edit the file to change status: "todo" -> status: "done")
git add agentprompts/QUEUE.yml
git commit -m "chore: mark task VAL-001 as done in queue"

# Push to the existing branch
git push origin copilot-queue-tasks
```

---

## Validation Checklist

Before completing this task, verify:

### Code Validation
- [ ] All acceptance criteria are met
- [ ] Code follows existing style and patterns
- [ ] No TypeScript errors: `npm run type-check` (or equivalent)
- [ ] No linting errors: `npm run lint` (or equivalent)
- [ ] All tests pass: `npm test` (or equivalent)
- [ ] New code has appropriate test coverage

### Security Validation (if applicable)
- [ ] Input validation is present where needed
- [ ] No hardcoded secrets or credentials
- [ ] Error messages don't leak sensitive information
- [ ] Paths and user input are sanitized
- [ ] Security best practices from SECURITY.md are followed

### Documentation Validation
- [ ] Code changes are documented with comments
- [ ] Public functions have JSDoc/TSDoc comments
- [ ] README or other docs updated if needed
- [ ] No broken links in documentation

### Git Validation
- [ ] All changes staged: `git status`
- [ ] Correct commit message used
- [ ] QUEUE.yml updated with new status
- [ ] Changes pushed to correct branch: `copilot-queue-tasks`
- [ ] No merge conflicts exist

---

## Handling Issues

### If You Can't Meet an Acceptance Criterion

1. Document which criterion cannot be met and why
2. Propose an alternative approach
3. Mark the task as "blocked" in QUEUE.yml instead of "done"
4. Add a `blocked_reason` field explaining the blocker
5. Wait for human review before proceeding

### If You Discover Related Issues

1. Stay focused on the current task
2. Document the related issue in a comment
3. Suggest adding a new task to QUEUE.yml
4. Do NOT fix related issues in this commit

### If Tests Fail

1. Identify which tests are failing and why
2. Determine if tests need updating or if code has bugs
3. Fix the appropriate component
4. Document the decision in commit message if tests were updated

---

## Example Workflow

Here's what a successful task execution looks like:

1. **Read source documents** (CODE_REVIEW_FINDINGS.md, FOLLOWUP_TASKS.md)
2. **Identify files to modify** (e.g., ProjectPulse/src/daemon/spawner.ts)
3. **Implement the fix** with proper error handling
4. **Write tests** (e.g., ProjectPulse/src/daemon/spawner.test.ts)
5. **Run tests** to verify they pass
6. **Update docs** if needed (add JSDoc comments)
7. **Commit changes** with the specified message
8. **Update QUEUE.yml** status to "done"
9. **Commit queue update**
10. **Push everything** to the branch

---

## Final Reminder

You are working on **ONE TASK ONLY**: `VAL-001 - Validate Agent Type at Runtime`

- Your changes must be **minimal and focused**
- All commits go to the **existing branch**: `copilot-queue-tasks`
- Do NOT create a new PR or branch
- Follow ALL acceptance criteria
- Update QUEUE.yml status when done

**Ready? Begin implementation!**

---

## Post-Execution Report Template

After completing the task, provide a summary in this format:

```markdown
## Task VAL-001 Completion Report

### Changes Made
- Modified: [list files changed]
- Added: [list files added]
- Tests: [list test files created/updated]

### Acceptance Criteria Status
- [x] Criterion 1: Met - [brief explanation]
- [x] Criterion 2: Met - [brief explanation]
- [ ] Criterion 3: Not met - [reason and suggestion]

### Test Results
- Unit tests: [X passed, Y failed, Z skipped]
- Integration tests: [status]
- Coverage: [percentage]

### Validation Status
- TypeScript: [✓ Pass / ✗ Fail]
- Linting: [✓ Pass / ✗ Fail]
- Tests: [✓ Pass / ✗ Fail]

### Commits Created
1. `fix: add runtime validation for agent types`
2. `chore: mark task VAL-001 as done in queue`

### Next Steps
- Human review required: [Yes/No]
- Issues discovered: [None / List any related issues found]
- Ready for next task: [Yes/No]
```

---

**Template Version:** 1.0.0  
**Last Updated:** 2026-01-27  
**Compatible with:** Project Pulse Queue v1.0.0
